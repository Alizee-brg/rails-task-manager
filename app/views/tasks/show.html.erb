<h1> Detail of one task </h1>
  <h2> <%= @task.completed%> - <%= @task.title%> </h2>
<%= link_to'Edit the task', edit_task_path(@task) %>
<%= link_to'Back to list', tasks_path(@task) %>
<%= link_to "Delete #{@task.title}", task_path(@task),method: :delete,data: { confirm: "Are you sure?" } %>
